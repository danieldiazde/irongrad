[project]
name = "irongrad"
version = "0.1.0"
description = "A from-scratch autograd engine and neural network framework in C++ and Python."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pybind11>=3.0.4",
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["build", "."]
